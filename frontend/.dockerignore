node_modules/
dist/

# Local dev-time mirror of card/pack art (see scripts/download-card-images.mjs).
# Deployed containers hotlink the dataset's own image URLs instead — see
# useLocalImage.ts's fallback — rather than shipping card/pack art baked into
# every image.
public/card-images/
public/pack-images/
